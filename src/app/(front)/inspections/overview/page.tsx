import {Container} from "@mantine/core"
import OverviewContainer from "./OverviewContainer";

const Page = () => {
  return (
    <Container>
      <OverviewContainer />
    </Container>
  );
};

export default Page;
